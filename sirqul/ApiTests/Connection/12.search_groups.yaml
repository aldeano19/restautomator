search_groups:
  details:
    ordinal: 1
    entry_point: "/connection/group/search"
    write_output: "search_groups_out"
    # confirm_output: True

  data:
    accountId:
      global_constants: ken_account_id
    
assert:
  isvalid:
    key: "valid"
    expected: True
    on_error_key: "message"
