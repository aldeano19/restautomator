create_retailer_location_for_create_retailer_location:
  details:
    ordinal: 1
    entry_point: /retailer/location/create
    confirm_output: True

  data:
    accountId:
      global_constants: ken_account_id
    name:
      global_constants: headquarter_name
    streetAddress:
      global_constants: headquarter_streetAddress
    city:
      global_constants: headquarter_city
    state:
      global_constants: headquarter_state
    postalCode:
      global_constants: headquarter_zip
    retailerId:
      global_constants: test_retailer_id

assert:
  isvalid:
    key: valid
    expected: True
    on_error_key: message
  
  ## TODO check why is this not working
  # checkname:
  #   key: [item, name]
  #   expected: Headquarters
  #   on_error_key: message