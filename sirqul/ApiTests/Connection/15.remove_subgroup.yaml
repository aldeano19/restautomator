remove_subgroup:
  details:
    ordinal: 1
    entry_point: "/consumer/connection/group/removeSubGroup"
    write_output: "remove_subgroup_out"
    # confirm_output: True

  data:
    accountId:
      global_constants: ken_account_id
    groupId:
      global_constants: parent_group_id
    subGroupIds:
      global_constants: child_group_id
    
assert:
  isvalid:
    key: "valid"
    expected: True
    on_error_key: "message"
