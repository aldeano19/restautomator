permission_leave_for_permission_leave:
  details:
    ordinal: 1
    entry_point: /consumer/permissions/remove
    # confirm_output: True
    description: user removes a connection from the permissionable object. | 3

  data:
    accountId:
      global_constants: ken_account_id
    permissionableType:
      global_constants: permissionable_type_album
    permissionableId:
      global_constants: permissionable_id
    connectionIds:
      global_constants: ken_and_mr_t_connection_id

assert:
  isvalid:
    key: valid
    expected: True
    on_error_key: message
