get_sent_pending_requests_for_get_sent_pending:
  details:
    ordinal: 1
    entry_point: /consumer/connection/getRequested
    description: Check for the request in pending requests. | 09
    # confirm_output: True

  data: 
    accountId:
      global_constants: ken_account_id

assert:
  isvalid:
    key: valid
    expected: True
    on_error_key: message
    silent: True

  hasrequestfromtcc:
    key: [connections, [username]]
    expected: tcc@testcasecentral.com
    on_error_key: [connections, [username]] # unlike 'key:' above, on_error_key: does not suport list format yet, so this will fail if ever executed
