add_subgroup:
  details:
    ordinal: 1
    entry_point: "/consumer/connection/group/addSubGroup"
    write_output: "add_subgroup_out"
    read_input: 
      - addsubgroup_create_parent_group
      - addsubgroup_create_child_group
    confirm_output: True

  data:
    accountId:
      global_constants: ken_account_id
    groupId:
    subGroupIds:

  dependencies:
    - ["addsubgroup_create_parent_group", "connectionGroupId", "groupId"]
    - ["addsubgroup_create_child_group", "connectionGroupId", "subGroupIds"]

assert:
  isvalid:
    key: "valid"
    expected: True
    on_error_key: "message"


