add_connection:
  details:
    ordinal: 1
    entry_point: "/consumer/connection/add"
    write_output: "add_connection_connection_out"
    # confirm_output: True

  data:
    accountId:
      global_constants: ken_account_id
    connectionId:
      global_constants: ken_and_mr_t_connection_id

assert:
  isvalid:
    key: "valid"
    expected: True
    on_error_key: "message"
