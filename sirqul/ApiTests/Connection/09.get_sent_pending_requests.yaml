pending_requests:
  details:
    ordinal: 1
    entry_point: "/consumer/connection/getRequested"
    write_output: "pending_requests_out"
    # confirm_output: True

  data:
    accountId:
      global_constants: ken_account_id
    

assert:
  isvalid:
    key: "valid"
    expected: True
    on_error_key: "message"
