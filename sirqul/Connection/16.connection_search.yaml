connection_search:
  details:
    ordinal: 1
    entry_point: "/connection/search"
    write_output: "connection_search_out"
    # confirm_output: True

  data:
    accountId:
      global_constants: ken_account_id

assert:
  isvalid:
    key: "valid"
    expected: True
    on_error_key: "message"
